# Domain Docs

This repository is a single-context project.

Before exploring a domain area, read the root `CONTEXT.md` when it exists and
the relevant records under `docs/adr/` when they exist. If those files are not
present, proceed without creating them as setup work. Use the project's domain
terms consistently; resolve missing vocabulary through domain modeling when
needed.
